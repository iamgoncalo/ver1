# Requirements ledger for the Versuni Air Treatment case (Project 1 only).
# Status values: PASS, PARTIAL, BLOCKED, NOT_APPLICABLE.
# Update this file, not STATUS.md, when a requirement's status changes;
# STATUS.md summarizes this file plus live command output.

data_evidence:
  consumer_reviews_real:
    status: PASS
    detail: "10,547 real Amazon reviews, 237 real hand-validated purifier products"
    source: data/raw/consumer_reviews.csv
  trend_corpus_real:
    status: PASS
    detail: "12 real, individually fetched and archived documents"
    source: data/raw/trend_corpus.json
  market_metrics_real:
    status: PASS
    detail: "2 real, archived, genuinely disagreeing sources (Mordor 5.37% vs IMARC 6.54%)"
    source: data/raw/market_metrics.json
  wtp_evidence:
    status: PASS
    detail: "Honestly marked NOT AVAILABLE (no direct/proxy WTP data exists); real
            price-exposure indicator reported separately, correctly labelled"
    source: data/processed/wtp_real.json

questions:
  Q1_framing_measures:
    status: PASS
    detail: "3 measures fixed in src/config.py::DECISION_METRICS, held throughout"
  Q2_data_quality:
    status: PASS
    detail: "Real defects found (not planted): 239 sentiment conflicts, 18 empty
            text, 0 duplicate bursts, 0 volume anomalies - all genuine findings"
  Q3_taxonomy:
    status: PARTIAL
    detail: "Real bottom-up taxonomy complete (6 themes). Human-label validation
            BLOCKED - data/hand_label_sample_BLANK.csv awaits a real human labeller."
    blocker: human_action
  Q4_wtp:
    status: PASS
    detail: "Honest 'not available' answer + real price-exposure proxy, correctly
            distinguished from WTP"
  Q5_market_disagreement:
    status: PASS
    detail: "2 real archived sources; executable --market-scenario sensitivity
            (CLI + dashboard Scenario Lab); verdict confirmed invariant"
  Q6_recommendation:
    status: PASS
    detail: "Three real Versuni dimensions - Consumer Pain (CSAT severity, gated
            by prevalence), Economic Value (price-weighted exposure), 2-5 Year
            Feasibility (ordinal, grounded in real trend-corpus citations).
            Winner computed via gate -> Pareto dominance -> named judgment rule
            (src/real/decision_framework_real.py::evaluate/dominates/break_tie),
            never a fixed literal. Current real-data result: NON_DOMINATED_PLUS_
            JUDGMENT, decision_priority=pain_feasibility_majority, winner OS-1
            (Reliability-Verified Air Purifiers). Proven genuinely dynamic by
            tests/test_dynamic_winner.py (8 tests: current-evidence winner,
            dominance-flips-winner, judgment-flip flips winner to OS-2 under
            --decision-priority=economic_value_override, order-independence,
            no hardcoded winner literal in src/real/) and by an independent
            review (found + this session fixed: a stale dashboard field
            reference (enabling_trend) after the schema change, an
            unvalidated decision_priority silently defaulting instead of
            raising, and an unreachable-in-practice empty-profiles crash -
            now a clear error instead)."

deliverables:
  insight_pack_5_slides: {status: PASS, source: deliverables/insight_pack.md}
  technical_note_2_pages: {status: PASS, source: deliverables/technical_note.md}
  evidence_table: {status: PASS, source: deliverables/evidence_table.csv}
  data_quality_report: {status: PASS, source: deliverables/data_quality_report.md}
  ai_use_log: {status: PASS, source: deliverables/ai_use_log.md}

control_room:
  makefile: {status: PASS, detail: "refresh/all/test/verify/app/live-check, all tested"}
  verification_engine:
    status: PASS
    detail: "scripts/verify_submission.py, 16 checks + 1 bonus, 229/229 passing;
            negative-test proof done this session (broke one row, verify failed,
            restored, verify passed)"
  claim_trace_tool:
    status: PASS
    detail: "scripts/trace_claim.py - single shared implementation, imported by
            both the CLI and dashboard EVIDENCE tab, no duplicate logic"
  dashboard:
    status: PASS
    detail: "dashboard/app.py, all 8 required tabs, verified via real browser
            session (not just headless): raw-review search, human-labelling
            with proven blinding, a real SCENARIO LAB run confirmed via git
            status to leave data/raw and data/processed untouched (writes
            only to data/runtime/scenario_result.json). NOTE: this
            untouched guarantee applies to the Scenario Lab specifically -
            the EVIDENCE tab's make test/verify/live-check buttons
            deliberately DO run the real CLI/pipeline and can rewrite
            data/processed/*.json, by design (rehearsing the exact live-
            session commands). An independent review of this layer also
            found and this session then fixed: (a) verdict['recommended']
            had been a hardcoded 'OS-1' literal despite scores being
            computed live - replaced with pick_winner(), a real comparator,
            proven to flip under synthetic score inputs; (b) OS-1/OS-2
            theme stats were sourced from the frozen snapshot even when a
            scenario passed filtered rows - fixed to recompute via
            compute_theme_stats()/compute_price_exposure() on the actual
            rows passed in; (c) a labelling-blinding leak where the
            Representative Excerpts widget could show a review's automated
            theme before its blind label was saved - fixed by excluding the
            50 blind-sample review_ids from that widget's pool."
  ci_workflow:
    status: PASS
    detail: ".github/workflows/verify.yml prepared locally, NOT pushed;
            runs make all/test/verify/live-check, no network/API keys needed"

reproducibility:
  offline_frozen_build: {status: PASS, detail: "make all reads data/real_raw/ only, no network"}
  network_refresh_path: {status: PASS, detail: "make refresh re-streams real source data; optional"}
  fixed_seeds: {status: PASS, detail: "src/config.py RANDOM_STATE=42 used wherever sampling occurs"}
  test_suite: {status: PASS, source: tests/test_real_pipeline.py}
  verification_engine: {status: PASS, source: scripts/verify_submission.py}

interview_readiness:
  live_rehearsal_doc: {status: PASS, source: LIVE_REHEARSAL.md}
  localhost_control_room: {status: PASS, source: dashboard/app.py}
  q5_scenario_lab: {status: PASS, detail: "same underlying function as CLI, verified"}
  claim_trace_tool: {status: PASS, source: scripts/trace_claim.py}
  human_labelling_ui: {status: PASS, detail: "blinded by construction; see dashboard CONSUMERS tab"}

blockers_requiring_a_human:
  - id: hand_label_50_reviews
    what: "Label data/hand_label_sample_BLANK.csv (50 real reviews) against the
          codebook in technical_note.md, save as data/manual/hand_labels.csv"
    who: goncalo
  - id: repo_visibility_decision
    what: "Repo is currently public; brief wants private. origin/main is also
          still at the OLD synthetic-data commit b6e96f8 - none of this
          session's real-data repair has been pushed. Decide before submission."
    who: goncalo
  - id: push_decision
    what: "All real-data-repair, control-room, and Q6-rework work is
          committed locally only (latest: 5531d48 on branch
          design/innovation-explorer), not pushed."
    who: goncalo

v1_innovation_explorer:
  # Honestly assessed 2026-08-26T20:31:44Z per user-requested governance
  # audit - do not upgrade this status without an actual browser
  # verification against CHECKLIST.md sections 14-17 and 20.
  status: NOT_STARTED
  detail: "web/src/ is empty - no App.tsx, no components, no worlds, no
          navigation, no process rail exist as code. Backend groundwork DOES
          exist and is smoke-tested working: api/main.py (FastAPI, serves
          real view-models + a live, non-mutating scenario-recompute
          endpoint), and three new real-data generators
          (src/real/rivals_real.py, magic_box_real.py,
          products_signals_real.py) producing genuinely computed
          data/processed/{rivals,white_space,magic_box,products,signals}_real.json.
          None of this is committed yet. The five-world visual experience
          itself has not been started."
  backend_smoke_tested:
    - "GET /api/health -> real git commit"
    - "GET /api/products -> 237 real products"
    - "GET /api/innovations -> OS-1 (matches CLI)"
    - "GET /api/innovations/scenario?decision_priority=economic_value_override -> OS-2 (live flip, matches CLI)"
  brand_asset:
    status: PASS
    detail: "web/public/brand/versuni-logo.png + SOURCE.md - real official
            asset from versuni.com/newsroom, hash-recorded, unmodified"
