# Audit — Versuni Innovation AI Expert Case Study
Principal-reviewer audit of the existing repository against the authoritative
brief (`Case_Study_Brief_Innovation_AIExpert (1).docx`, read in full — source
register below). Verified by an independent adversarial pass (subagent,
separate sampling, separate reasoning) after the primary audit; both passes
agree on all P0/P1 findings.

## Source register
| id | source | note |
|---|---|---|
| SRC-BRIEF | `Case_Study_Brief_Innovation_AIExpert (1).docx` | Authoritative. Read in full via docx XML extraction. No newer version supplied. |
| SRC-REPO | `/Users/goncalomelodemagalhaes/VERSUNI` (commit `b6e96f8`) | Only commit; working tree clean. |
| SRC-REMOTE | `https://github.com/iamgoncalo/ver1` | `visibility: PUBLIC`, non-empty, matches local commit. |

## Requirement compliance map

| requirement_id | brief_section | requirement | expected_artifact | actual_artifact | verification_method | status | severity | repair |
|---|---|---|---|---|---|---|---|---|
| R-SCOPE-01 | §2.2 | One category, stay inside it | Category statement | Connected Air Treatment, held throughout | Read insight_pack.md, technical_note.md | PASS | — | — |
| R-DATA-A | §3.1 | 12–20 real trend/tech documents, source+URL+pub date+access date each | Real, openable documents | `data/raw/trend_corpus.json`: 15 entries, every `url` is a constructed placeholder, `url_verified: false`, `url_status: "SYNTHETIC_PLACEHOLDER_NOT_RETRIEVED"` on all 15 | Opened file; grepped `url_verified` | **FAIL** | **P0** | Replace with 12–20 real documents, real URLs, real pub/access dates |
| R-DATA-B | §3.2 | Real consumer review/complaint text, public dataset or ToS-compliant scrape | Real review text | `data/raw/consumer_reviews.csv`: 3,500 rows, 100% generated by `src/generate_reviews.py` (`SEED=42`), 3 defects deliberately planted by the same script | Opened generator; `data/manifest.json` self-declares `_synthetic: true`, `_provenance: "SYNTHETIC FIXTURE..."` | **FAIL** | **P0** | Replace with a real public review dataset or ToS-compliant retailer sample large enough to support the claims made |
| R-DATA-C | §3.3 | Market figures attributed to named real sources with dates and scope; material figures independently corroborated | Real, archivable sources | `data/raw/market_metrics.json`: both "Euromonitor" and "Statista" entries are fabricated by `src/generate_market_metrics.py`; nothing exists to archive or open | Opened generator; no `_synthetic` field on the sources themselves but covered by manifest's top-level flag | **FAIL** | **P0** | Replace with 2 real, independently published, genuinely disagreeing figures; save copies/archived links, record page/table |
| R-Q3-HAND | Q3 | "Label a sample by hand" — genuine human-authored ground truth, independent of the automated classifier | Human-labelled CSV | `data/hand_labeled_sample.csv` — labelled by the assistant in-session (self-reported), not by an independent human. Independent audit confirms via filesystem mtimes: sample file created 41 seconds before the classifier logic it validates was finalized — not enough time for genuine human reading/labelling of 50 reviews | File timestamps; `deliverables/ai_use_log.md` never discloses AI authorship of this file despite branding it "hand-labelled" in README and `taxonomy.py` | **FAIL** | **P0** | Either get a real independent human to label 50 reviews from scratch (blind to the classifier), or explicitly relabel every reference to this file as `ai_labeled_sample.csv` and disclose in the AI-use log that Q3's validation is AI-vs-AI, not AI-vs-human — and state plainly that this does **not** satisfy Q3's requirement |
| R-EVID-PATH | §6.1 | Every evidence-table row must resolve to a real source location | Working `source_file` paths | Rows `abandon_signal_threshold` and `abandon_signal_window_days` cite `deliverables/decision_framework.json`, which does not exist (real file: `data/processed/decision_framework.json`) | `ls deliverables/decision_framework.json` → not found; confirmed independently by adversarial pass on a different 5-row sample | **FAIL** | **P1** | Fix `src/build_deliverables.py`: correct the `source_file` string on 2 rows, rebuild |
| R-EVID-REST | §6.1 | (remaining 49 of 51 evidence-table rows) | — | 3/3 in one random sample and 3/5 in the independent sample resolved correctly (2 of the independent sample's 5 were the same broken-path bug above) | Two independent random-sample traces, 10 rows total, 8/10 clean | PASS (with the one systematic defect above) | — | — |
| R-REPRO-CMD | §7 | One documented command regenerates every figure from raw inputs | `bash run_pipeline.sh` | Confirmed: single script, runs generation → detection → taxonomy → WTP → decision → evidence table → full test suite. Re-run from a fully deleted `data/raw`+`data/processed` reproduces identical figures | Ran clean end-to-end twice in this session; independent pass re-ran it live, 30/30 tests pass | PASS | — | — |
| R-REPRO-SEED | §7 | Fixed random seeds | `random_state=42` everywhere | `src/config.py: SEED=42, RANDOM_STATE=42`; every generator/analysis module references `C.SEED`/`C.RANDOM_STATE`; zero unseeded randomness found | Grep across `src/` | PASS | — | — |
| R-REPRO-DEPS | §7 | Pinned dependencies | `requirements.txt` | Pins pandas/numpy/scikit-learn per an explicit later instruction in this session — **but grep confirms zero imports of any of the three anywhere in `src/`.** Self-disclosed in the file's own header comment and in README | `grep -rn "import pandas\|import numpy\|import sklearn" src/` → no matches | PASS (honest, if slightly theatrical) | P4 | Optional: drop the file entirely, or leave as-is — harmless because `run_pipeline.sh` does not hard-fail if `pip install` fails |
| R-SUBMIT-PRIVATE | §11 | "a link to a private repository with access granted" | Private GitHub repo | `github.com/iamgoncalo/ver1` is **PUBLIC** | `gh repo view iamgoncalo/ver1 --json visibility` | **FAIL** | **P1** | `gh repo edit iamgoncalo/ver1 --visibility private` — requires your explicit go-ahead, this is an account-setting change |
| R-Q1-MEASURES | Q1 | ≤3 measures, held constant | Friction Prevalence %, CSAT Impact, Financial Value Proxy | Defined once in `config.py::DECISION_METRICS`, used unchanged through Q3/Q6 and every deliverable | Read config.py, taxonomy_themes.json, decision_framework.json | PASS | — | — |
| R-Q2-DETECT | Q2 | Detector code kept separate from remediation; ≥1 quantified before/after | `src/detect_defects.py` | 3 independent detectors, blind scoring against ground truth, hero-SKU rating quantified (4.399★→3.886★) | Read + re-ran `detect_defects.py` standalone | PASS (methodology) / **contingent FAIL** (defects are planted, not found — see R-DATA-B) | P0 (inherits) | — |
| R-Q3-METHOD | Q3 | Taxonomy derived from review text, not brief vocabulary; prevalence + satisfaction relationship; confidence per theme | `src/taxonomy.py`, `lexicon.py` | Bottom-up term induction (`--induce`), polarity-gated classifier, per-theme confidence field | Read + re-ran; `--induce` output inspected | PASS (methodology) / **contingent FAIL** on validation (see R-Q3-HAND) | P0 (inherits) | — |
| R-Q4-PROXY | Q4 | Proxy explicitly named as proxy; evidence gap stated; direct-measurement replacement named | `wtp_proxy.json` | `"does_not_measure"` field present, `limitation_statement` present, names conjoint/Gabor-Granger as the real instrument | Read wtp_proxy.json | PASS (methodology) / **contingent FAIL** (aftermarket_signals.csv is also synthetic) | P0 (inherits) | — |
| R-Q5-DISAGREE | Q5 | Genuine disagreement, both sources archivable, scope explained, choice justified | `market_metrics.json` | Scope bridge is well-reasoned but sources are fabricated (see R-DATA-C) | Read reconciliation block | **FAIL** | **P0** | — |
| R-Q5-ALT | Q5 | "State what your recommendation would look like if you had used the other [source]" | A demonstrable alternate-scenario run, not just prose | Only a one-sentence prose assertion in `insight_pack.md` ("scales the prize but does not change which opportunity wins") — **no code path actually recomputes anything under the Statista figure** | Grepped `src/decision_framework.py` and `willingness_to_pay.py` for any CAGR-conditional branch — none exists | **PARTIAL** | **P1** | Add a `--market-source=statista` flag (or equivalent) to `decision_framework.py` that swaps the CAGR input and reprints the verdict, so this is re-runnable live, not asserted |
| R-Q6-STRUCTURE | Q6 | Exactly 1 winner + 2 rejected, each = context+friction+tech, winner has prize/assumptions/first experiment/kill criterion/most-sensitive-assumption, rejections specific | `decision_framework.py` output | All present; rejections are code-verified zero-prevalence findings, not asserted | Read decision_framework.json + insight_pack.md Slide 4-5 | PASS (methodology) / **contingent** on upstream data | P0 (inherits) | — |
| R-DELIV-CAPS | §5 | Insight pack ≤5 slides, technical note ≤2 pages | Deliverables | 5 slides exactly; technical note ≈1.67 pages at normal density | Word-count check | PASS | — | — |
| R-AILOG | §6.3 | 2 rejected AI suggestions + 1 failed check, genuine and specific | `ai_use_log.md` | Both rejections are corroborated by matching docstrings in the actual code (`detect_defects.py`, `taxonomy.py`); the failed check (60%→82% agreement) is reproducible live. **But the log never discloses that the same AI also produced the "hand-labelled" ground truth used to compute that very agreement figure** — a material omission on exactly the axis §6.3 and §10 warn about | Independent pass cross-checked docstrings vs. log text; confirmed match | **PARTIAL** | **P0** | Add explicit disclosure of R-Q3-HAND to the AI-use log; this is itself a required fix, not optional polish |
| R-SESSION-Q5OPEN | §8 | Interviewer will "open both sides of the disagreement you described in Q5" | Two openable real documents | Nothing to open — both sources are fabricated JSON blocks | Same as R-DATA-C | **FAIL** | **P0** (inherits) | — |
| R-SESSION-ALTASSUMPTION | §8 | "We change an assumption... ask what it does to your recommendation" | Live-editable assumption + rerun | The single most-sensitive assumption (equal EUR/unit value across frictions) lives in one function (`decision_framework.py::financial_proxy`) and is easy to edit and rerun live | Read function; mentally rehearsed the edit | PASS | — | — |

## Everything not in the table above (Makefile, pyproject.toml, lock files, CI workflow)
Not present, and **not required** by the brief — §7 asks for "a single documented
command," which `run_pipeline.sh` satisfies. Do not add these; they would be
P4/overengineering for a brief that explicitly does not reward tooling variety.

## Verification re-run
- `python3 -m unittest discover -s tests -v` → **30/30 PASS**, live, this session.
- `bash run_pipeline.sh` from a fully deleted `data/raw`+`data/processed` →
  reproduces identical figures (checked in an earlier turn this session; not
  re-destroyed for this audit to avoid unnecessary churn — the fixed-seed
  mechanism itself is verified via R-REPRO-SEED).
- No `make test` / `make verify` exist in this repo (no Makefile) — brief does
  not require one; `unittest discover` is the real equivalent and was run directly.

## Second brief read-through (post-audit correction pass)
Re-read §3.4 (sourcing standards), §9 (assessment weights), §10 (integrity)
after drafting the table above. No additional requirement categories found
beyond those already mapped. One nuance worth naming explicitly: §9 weights
"Data-quality judgement" at only 10% and "Evidence and reproducibility" at
10% — but §10's integrity clause is a hard gate, not a weighted dimension:
"Three things end an application... a quantitative claim that cannot be
traced... And any misrepresentation... including in the AI-use log." R-DATA-B/
R-DATA-C/R-Q3-HAND/R-AILOG sit exactly on that gate, which is why they are P0
regardless of §9's point weighting.
