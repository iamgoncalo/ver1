# Real, verified runtime dependencies of this project - every entry below
# is genuinely imported by code that runs (import-audited; nothing is kept
# "just in case").

# Web application: api/main.py (FastAPI app) served by uvicorn (make app /
# the production container).
fastapi==0.115.0
uvicorn==0.30.6

# Research/consumer clustering: emergent_clustering_real.py and
# intelligence_fabric.py import TfidfVectorizer / AgglomerativeClustering /
# cosine_similarity - run by `make all` (stages 8c/8i).
scikit-learn==1.5.0
# scikit-learn's own hard dependency, pinned for reproducible installs.
numpy==1.26.4

# Innovation Disclosure PDFs: generate_innovation_disclosures.py
# (run_pipeline.sh stage 8k).
reportlab==4.2.2

# Local analyst control room only: dashboard/app.py (`make analyst`).
# Not needed by the production website container.
streamlit==1.38.0
