__pycache__/
*.pyc
.DS_Store

# Scenario Lab scratch output - regenerated per session, not submission
# evidence. data/manual/hand_labels.csv is NOT ignored - that one is real.
data/runtime/*.json

# Frontend build artifacts - reproducible via `cd web && npm install && npm run build`
web/node_modules/
web/dist/
