# src/ uses ONLY the Python 3.9+ standard library for the analysis pipeline
# (csv, json, re, random, hashlib, datetime) - none of pandas/numpy/
# scikit-learn below are imported by anything under src/. `make all` does
# not depend on any of them.
#
# streamlit IS a real dependency: dashboard/app.py (`make app`), the local
# control room. Pinned to the version this repo was built and tested
# against.
streamlit==1.38.0

# Pinned per an earlier explicit instruction in this session, kept in case
# a future step needs them; harmless if unused - `make all`/`make refresh`
# do not fail if these can't install (no network required for `make all`).
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
