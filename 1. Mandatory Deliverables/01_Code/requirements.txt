# src/real/ uses the Python 3.9+ standard library (csv, json, re, random,
# hashlib, datetime) for everything except one step: the research-paper
# and consumer-friction clustering in emergent_clustering_real.py and
# intelligence_fabric.py, which genuinely imports scikit-learn
# (TfidfVectorizer, AgglomerativeClustering, cosine_similarity). Both are
# run by `bash run_pipeline.sh` (stages 8c/8i) - scikit-learn is a real,
# required runtime dependency, not an optional or unused one.
scikit-learn==1.5.0

# scikit-learn's own hard dependency (not imported directly by anything
# under src/) - pinned explicitly for reproducible installs.
numpy==1.26.4
