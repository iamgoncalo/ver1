# Pinned per request. NOTE: as of this commit, src/ uses ONLY the Python 3.9+
# standard library (csv, json, re, random, hashlib, datetime) — nothing below
# is currently imported anywhere in the pipeline. They are pinned here so the
# environment is ready if a future step (e.g. a heavier taxonomy model) needs
# them, and run_pipeline.sh does not hard-fail if `pip install` has no network
# access, since the actual analysis does not depend on it.
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
